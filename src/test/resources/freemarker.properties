# FreeMarker Configuration to suppress debug logging
# Used by ExtentReports for HTML report generation

# Disable debug output
debug=false

# Disable template cache debug logging
cache_storage=freemarker.cache.MruCacheStorage(2000, 60)
